# Quản lý
- Cho phép quản lý `Khóa tài khoản` và `Cấp lại mật khẩu` cho các tài khoản có vai trò `Seller`, `Khách hàng`.
- Không cho phép quản lý `Thêm tài khoản quản lý`.

# Khách hàng
- Khi mua hàng ở nhiều shop khác nhau, tiền ship hiển thị ở trang `Xác nhận đặt hàng ngay` chỉ hiển thị tiền ship cố định`. Hãy sửa lại để tiền ship tính dựa trên số lượng shop, lượng hóa đơn tách ra.
- Popup `Xem hóa đơn` bị popup `Lịch sử đơn` che khuất. Chỉnh popup `Xem hóa đơn` lên cao hơn.
- Bỏ tab `Chỉnh sửa` trong popup hiện lên khi bấm button `Tài khoản`.
- Bỏ button `Tài khoản`.
- Bỏ nút `📋 Xem lịch sử đơn hàng` trong popup hiện lên khi bấm button `Tài khoản`.